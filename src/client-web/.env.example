# Azure Storage Configuration
AZURE_STORAGE_ACCOUNT_NAME=your-storage-account-name

# Azure Service Bus Configuration  
AZURE_SERVICE_BUS_FQDN=your-service-bus-namespace.servicebus.windows.net
AZURE_SERVICE_BUS_TOPIC_NAME=email-events

# Application Configuration
HOST=0.0.0.0
PORT=8000

# Azure Authentication (uses DefaultAzureCredential)
# Ensure you are logged in with 'az login' for local development